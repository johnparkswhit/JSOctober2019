/* Literals
JS defines two types of values: Fixed values and Variable values.
Fixed values are called literals, and Variable values are variables.
*/

let days = ['Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday'];
console.log(days.toString()); //method
console.log(days.length); //property

let tired = true;

let soup = {                //an object
    a:'chicken noodle',     //key value pairs
    b:'tomato',
    c:'beef and barley',
}
console.log(soup.a); //dot notation
console.log(soup.a.length);
console.log(typeof soup.c);
console.log(soup.a.toUpperCase);



let car = 'Honda';

let dec = 10.2;
